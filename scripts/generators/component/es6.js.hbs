import React, { Component, PropTypes } from 'react';

{{#if wantCSS}}
import css from './{{ properCase name }}.css';
{{/if}}

class {{ properCase name }} extends Component {
    render() {
        return (
            <div />
        );
    }
}

{{ properCase name }}.propTypes = {

};

export default {{ properCase name }};
